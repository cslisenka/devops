1. Install Oracke JDK 8
sudo add-apt-repository ppa:webupd8team/java
sudo apt-get update
sudo apt-get install oracle-java8-installer

2. Install tomcat
2.1 Create user and directory
sudo useradd tomcat -p tomcat
sudo mkdir /opt/tomcat
sudo chown -R tomcat /opt/tomcat

2.2 Install tomcat
sudo su - tomcat
cd /opt/tomcat
sudo wget http://ftp.byfly.by/pub/apache.org/tomcat/tomcat-9/v9.0.5/bin/apache-tomcat-9.0.5.tar.gz
tar -xvzf apache-tomcat-9.0.5.tar.gz

2.3 Configuring tomcat
/opt/tomcat/apache-tomcat-9.0.5/conf/server.xml
    <Connector address="127.0.0.1" port="8080" protocol="HTTP/1.1"
               connectionTimeout="20000"
               redirectPort="8443" />

2.4 Run tomcat
cd /opt/tomcat/apache-tomcat-9.0.5/bin
./startup.sh

3. Install nginx
sudo apt-get install nginx
systemctl status nginx

3.1 Configure nginx
move nginx.conf to /etc/nginx/sites-available/tomcat
create symlink to it at /etc/nginx/sites-enabled
sudo service nginx restart

3.2 SSL in nginx
sudo mkdir /etc/nginx/ssl
sudo openssl genrsa -des3 -out private.pem
sudo openssl req -new -x509 -key private.pem -out cert.pem -days 10950
create file /etc/nginx/ssl/password with password
copy file nginx-ssl.conf to /etc/nginx/sites-available/tomcat
create symlink to it at /etc/nginx/sites-enabled